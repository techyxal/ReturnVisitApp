<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base application theme -->

        <!-- Base application theme. -->
        <style name="Base.Theme.RV0" parent="Theme.Material3.DayNight.NoActionBar">
            <!-- Customize your theme here. -->
        </style>

        <!-- Base style for your app's buttons -->
        <style name="Widget.App.Button" parent="Widget.Material3.Button">
            <item name="android:layout_width">match_parent</item>
            <item name="android:layout_height">wrap_content</item>
            <item name="android:layout_marginBottom">16dp</item>
            <item name="android:insetTop">0dp</item>
            <item name="android:insetBottom">0dp</item>
            <item name="android:paddingTop">12dp</item>
            <item name="android:paddingBottom">12dp</item>
            <item name="iconPadding">12dp</item> <!-- Spacing between icon and text -->
        </style>

        <!-- Primary Action Button (e.g., Filled) -->
    <style name="Widget.App.Button.Primary" parent="Widget.Material3.Button">
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:paddingTop">12dp</item>
        <item name="android:paddingBottom">12dp</item>
        <item name="iconPadding">12dp</item>
        <!-- Add other common attributes like corner radius, etc. -->
    </style>

    <style name="Widget.App.Button.Secondary" parent="Widget.Material3.Button.OutlinedButton">
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:paddingTop">12dp</item>
        <item name="android:paddingBottom">12dp</item>
        <item name="iconPadding">12dp</item>
        <!-- Add other common attributes -->
    </style>
    <!-- ... other styles you might have ... -->

    <!-- Base style for your app's buttons if you have one -->
    <!-- <style name="Widget.App.Button" parent="Widget.Material3.Button"> -->
        <!-- Common attributes for all your buttons -->
    <!-- </style> -->

    <!-- Primary Action Button (example) -->
    <!-- <style name="Widget.App.Button.Primary" parent="Widget.Material3.Button"> -->
        <!-- ... -->
    <!-- </style> -->

    <!-- Secondary Action Button (example) -->
    <!-- <style name="Widget.App.Button.Secondary" parent="Widget.Material3.Button.OutlinedButton"> -->
        <!-- ... -->
    <!-- </style> -->

    <!-- ADD OR VERIFY THIS DEFINITION -->
    <style name="Widget.App.Button.Tertiary" parent="Widget.Material3.Button.TextButton">
        <!-- Common attributes from your other app buttons that should apply -->
        <!-- Or define specific attributes for tertiary buttons -->
        <item name="android:layout_width">wrap_content
        </item> <!-- Text buttons are often wrap_content -->
        <item name="android:layout_height">wrap_content</item>
        <item name="android:paddingStart">@dimen/button_tertiary_padding_horizontal
        </item> <!-- Example -->
        <item name="android:paddingEnd">@dimen/button_tertiary_padding_horizontal
        </item>   <!-- Example -->
        <item name="android:paddingTop">@dimen/button_tertiary_padding_vertical
        </item>     <!-- Example -->
        <item name="android:paddingBottom">@dimen/button_tertiary_padding_vertical
        </item>   <!-- Example -->
        <item name="iconPadding">@dimen/button_tertiary_icon_padding
        </item>                 <!-- Example -->
        <!-- Add other common attributes like textAllCaps, textAppearance, etc. if needed -->
    </style>
        <!-- Base application theme. -->
    <style name="Base.Theme.RV0a" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" >?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>


        </resources>